import pygame as pg

class Controller:
    def __init__(self, config, screen, clock):
        self.config = config
        self.screen = screen
        self.clock = clock

    def run(self):
        pass
